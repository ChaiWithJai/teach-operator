{
  "name": "adhd-copilot",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Local-first generative UI control plane for ADHD brains, on top of Codex.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "svelte": "^5.19.0",
    "vite": "^6.0.7"
  }
}
